/*
  This is a sample config file
  
  Replace the user/pass/endpoint with the one you want to use
  for the project and remove "-example" from the file name.
  This prevents this config from being stored in version control
 */
module.exports = {
  dbUser: '',
  dbPassword: '',
  dbEndpoint: ''
}